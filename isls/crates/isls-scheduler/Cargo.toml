[package]
name = "isls-scheduler"
version = "1.0.0"
edition = "2021"
description = "ISLS Spiral Scheduler — C15: adaptive tick granularity"

[dependencies]
isls-types = { path = "../isls-types" }
serde = { workspace = true }
